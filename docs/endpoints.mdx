---
id: endpoints 
---
import HttpMethodTag from '@site/src/components/HttpMethodTag/HttpMethodTag';
import { Paths } from '@site/src/constants/paths';
import { Endpoint } from '@site/src/constants/endpointPath';


# Endpoints
Here is a list of the scopes you require to access the API Endpoints and Webhook Events.
## Login

<div className="endpoint-list">
  <div className="highlight-box post">
    <HttpMethodTag method={'POST'} /> <a href={`./${Paths.otp}`}>**{Endpoint.otp}**</a>
  </div>

  
  <div className="highlight-box post">
    <HttpMethodTag method={'POST'} /> <a href={`./${Paths.phone}`}>**{Endpoint.login}**</a>
  </div>

  <div className="highlight-box post">
    <HttpMethodTag method={'POST'} /> <a href={`./${Paths.updateToken}`}>**{Endpoint.updateToken}**</a>
  </div>

  <div className="highlight-box post">
    <HttpMethodTag method={'POST'} /> <a href={`./${Paths.logout}`}>**{Endpoint.logout}**</a>
  </div>

</div>

## User

<div className="endpoint-list">
  <div className="highlight-box get">
    <HttpMethodTag method={'GET'} /> <a href={`./${Paths.user}`}>**{Endpoint.user}**</a>
  </div>
</div>
<div className="endpoint-list">
  <div className="highlight-box get">
    <HttpMethodTag method={'POST'} /> <a href={`./${Paths.contact}`}>**{Endpoint.contact}**</a>
  </div>
</div>

## Transactions

<div className="endpoint-list">
  <div className="highlight-box get">
    <HttpMethodTag method={'GET'} /> <a href={`./${Paths.transactionsAll}`}>**{Endpoint.transactionsAll}**</a>
  </div>
  <div className="highlight-box get">
    <HttpMethodTag method={'GET'} /> <a href={`./${Paths.transactionIndividual}`}>**{Endpoint.transactionIndividual}**</a>
  </div>
</div>






## Coins

<div className="endpoint-list">
  <div className="highlight-box get">
    <HttpMethodTag method={'GET'} /> <a href={`./${Paths.coinsAll}`}>**{Endpoint.coinsAll}**</a>
  </div>

  <div className="highlight-box get">
    <HttpMethodTag method={'GET'} /> <a href={`./${Paths.coinsIndividual}`}>**{Endpoint.coinsIndividual}**</a>
  </div>
  
  <div className="highlight-box get">
    <HttpMethodTag method={'POST'} /> <a href={`./${Paths.coinsRedeem}`}>**{Endpoint.coinsRedeem}**</a>
  </div>

  <div className="highlight-box get">
    <HttpMethodTag method={'POST'} /> <a href={`./${Paths.coinsDrop}`}>**{Endpoint.coinsDrop}**</a>
  </div>
  <div className="highlight-box get">
    <HttpMethodTag method={'GET'} /> <a href={`./${Paths.coinsPending}`}>**{Endpoint.coinsPending}**</a>
  </div>
  <div className="highlight-box get">
    <HttpMethodTag method={'GET'} /> <a href={`./${Paths.coinsNumberToCollect}`}>**{Endpoint.coinsNumberToCollect}**</a>
  </div>
</div>












